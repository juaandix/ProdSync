# Copia este archivo como .env.local y ajusta los valores según tu entorno.
# Este archivo es solo de referencia y no se usa directamente.

# URL base de la API del backend (Spring Boot) — solo necesaria en desarrollo local
# En Docker, esta variable la inyecta docker-compose en el contenedor del frontend.
API_URL=http://localhost:8080/api
